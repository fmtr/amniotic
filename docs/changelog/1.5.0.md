- Migrate Home Assistant Add-On base images from Alpine to Debian. This unifies the Add-On with the pre-build Docker images - and will hopefully resolve some glibc/musl-related crashes that occurred under Alpine.
